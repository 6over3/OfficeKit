# Upstream Open XML SDK fixtures

These files come from `dotnet/Open-XML-SDK` at commit
`cd2b359ef824737edb93f1c6157c19551aae1e52` (2026-07-21).

Paths under `Open-XML-SDK/` match paths under the upstream
`test/DocumentFormat.OpenXml.Tests.Assets/assets/` directory. A translated test must name its C#
test and source path. Add a file only when a test first uses it. Add its SHA-256 hash to
`SHA256SUMS` in the same change.

See `Open-XML-SDK-LICENSE` and `Open-XML-SDK-NOTICE` for license and notice text.
